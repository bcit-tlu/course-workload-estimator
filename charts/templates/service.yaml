apiVersion: v1
kind: Service
metadata:
  name: {{ include "course-workload-estimator.fullname" . }}
  labels:
    {{- include "course-workload-estimator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "course-workload-estimator.selectorLabels" . | nindent 4 }}
